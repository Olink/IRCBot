This project assumes you have IRC Lib copied into a folder "IRCLib" at the same
level as the main python files.  It also uses http://pytools.codeplex.com/releases

The project is a Visual Studio 2010 project.